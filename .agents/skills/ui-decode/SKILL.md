---
name: ui-decode
description: 复刻/对齐设计稿、还原 UI 界面时使用。讲只复刻界面实际渲染态、剔除隐藏弃用件的原则。
---

# 设计稿 / UI 复刻

- **只复刻界面实际渲染态**，剔除所有隐藏/弃用部分（`display:none` 等）——源码里存在 ≠ 界面上能看到。
- 动手前**先排查全部隐藏/弃用补丁**，搞清真实渲染态再写，别把废弃结构搬出来。
- 复刻产物（解码版/方案）按 `bug-fix-flow` 的归置规则放 `tmp_docs/`。
