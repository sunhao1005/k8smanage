# 项目规则

本项目的协作规则见 @AGENTS.md（与 Codex 共用同一份真源）。

@AGENTS.md

> Claude 专属提示：
> - 详细流程性规则放在 `.agents/skills/<名>/SKILL.md`，按 AGENTS.md 末尾「技能索引」在命中场景时 `Read` 对应文件，避免一次性占用上下文。
> - 强制规则由 `.claude/settings.json` 的 hooks 兜底（若已启用）。
